[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aerospace-structures-calculation"
version = "0.1.0"
description = "Standalone Python migration of the Assignment 1 Calculation.xlsx model"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

